"""Example profile: the minimal possible profile.

Every setting you omit falls back to the built-in defaults. With no
blacklists declared, nothing is filtered and every file is exported.
For small projects where you literally want it all - and proof that a
profile can be a single line of configuration.
"""

CONFIG_DESCRIPTION = "Raw dump: every file, no filters (tiny projects)"
